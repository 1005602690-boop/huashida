# 2026-08-18 工作记录

## 华仕达智能机械设备官网（静态站点）
- 任务：根据需求文档搭建企业品牌展示+营销获客官网（7 个一级栏目）。
- 技术栈：纯静态 HTML/CSS/JS（无框架），响应式 + 中英双语切换（localStorage 记忆）。
- 文件结构：
  - `index.html` 首页（首屏Banner/核心优势/产品入口/案例/新闻/CTA）
  - `about.html` 关于我们（概况/时间轴/生产实力/资质/团队）
  - `products.html` 产品中心（5 大系列+分类筛选+询盘弹窗）
  - `solutions.html` 解决方案（6 行业，背景→方案→效果）
  - `resources.html` 技术资料（白皮书/说明书/FAQ/文章）
  - `news.html` 新闻资讯（分类筛选）
  - `contact.html` 联系我们（信息+地图示意+留言表单）
  - `css/style.css` 设计系统（深蓝+金属灰+科技蓝）
  - `js/main.js` 交互（导航/双语/筛选/询盘/表单/返回顶部）
- 设计：深蓝 #0a2a4a + 金属灰 + 科技蓝 #1f9bff；3 秒内呈现"专业证据链"。
- 图片用内联 SVG 占位（无真实产品照）；地图为示意位置（需来电获取精准定位）。
- 本地预览：`python3 -m http.server 8080`（已在后台运行，http://localhost:8080）。
- 待办（用户可能要的后续）：真实产品图/视频、SEO 部署、CMS 后台、在线客服接入、地图嵌入。

## 下午补充：产品图替换 + 文案校准（百度爱采购）
- 用户要求：根据百度爱采购华仕达真实产品信息，替换产品图与视频。
- 取数：WebSearch 确认华仕达爱采购主营——高周波塑胶熔接机、吸塑包装封口机、高周波自动圆盘熔断机、高频热压机、超声波塑焊机、全自动 PVC/PET 圆筒成型机、旋熔/热板焊接机械、超声波/高周波模具。
- AI 生成 5 张系列实景图（`assets/images/`：`hf-machine.png` `ultrasonic.png` `automation.png` `packaging.png` `tooling.png`），原始长文件名已重命名为语义化名。
- 替换方式：纯 CSS 属性选择器，零 HTML 改动。
  - `.product-card[data-cat]` 按系列挂背景图（products.html 12 个产品卡）。
  - `.cat-grid > a:nth-child(n)` 首页 5 个产品入口分类卡背景。
  - `.case-grid > .case-card:nth-child(n)` 首页 3 个案例卡背景。
  - 原占位 SVG 设 `display:none`；缩略图加 `background-size:cover; position:center`。
- 文案校准：products.html 12 个产品卡描述已全部贴合爱采购主营；meta description 与 page-hero 加"百度爱采购入驻工厂直供"背书；首页 hero 副标题也加该背书。
- 视频：用户确认**不生成视频**。首屏 hero 占位 SVG 改为真实 `automation.png` 生产线实景图（`.hero__visual img` 加 `object-fit:cover`）。
- 服务器坑：8080 端口后台进程工作目录漂移导致 HTML 404（CSS/图 200），改用 **8081** 端口正常（http://localhost:8081）。后续预览用 8081。
- 注意：AI 图为示意素材，非华仕达实拍；百度爱采购平台真实照片/视频有版权，未直接抓取。

## 晚间补充：接入真实地图 + 在线客服
- 用户要求：接入真实地图、在线客服；详细地址 = 广东省东莞市凤岗镇风清路7号1号108室。
- 合规：触发 geo-map-compliance-guard skill。地图只能用工信白名单（腾讯/高德/百度/天地图）。采用**腾讯地图 GL JS 代理模式（默认场景，无前端 key）**，通过 `_TMapSecurityConfig.serviceHost` 指向 WorkBuddy 运行时占位符 `__WB_HTTP_PORT__` / `__WB_TMAP_SECRET__`，由运行时注入。仅 contact.html 引入 SDK（head 配 proxy，body 末尾 `gljs?v=1.exp&libraries=service` + main.js）。
- 实现：
  - contact.html：地址文案更新为真实门牌；`.map-box#map` 真实地图容器；新增腾讯/高德/百度"在地图中打开"导航按钮（按地址关键字拼 URI）。
  - main.js：`initContactMap()` 仅在 #map 存在时运行——创建地图(fallback 凤岗镇中心 GCJ-02 22.8722,114.0765) + 内联 SVG pin marker + InfoWindow；并用 `TMap.service.Geocoder` 地理编码精修到真实门牌（代理模式无需前端 key）。
  - main.js：`injectCustomerService()` 全局注入右下角客服浮窗（所有页面，因 main.js 全局加载）——含罗先生/孙小姐电话直拨、微信咨询(占位二维码 SVG，提示替换为真实)、邮箱 mailto、在线留言(联系页平滑滚动到表单/其他页跳 contact.html)、服务时间；支持双语切换。
  - style.css：`.map-box` 改为地图容器(340px)，新增 `.map-actions`、`.btn--sm/.btn--ghost`、完整的 `.cs-widget` 客服浮窗样式 + 移动端适配。
- 坑/说明：本地 python 服务器(8081)预览时，腾讯地图代理无 WorkBuddy 运行时注入，地图会灰屏/占位；**真实地图需在 WorkBuddy 预览(present_files)或部署时配置自有腾讯地图 key（非默认场景，替换占位符为真实 key）**。客服浮窗在任何本地服务器都正常。
- 交付预览用 present_files 呈现 contact.html（含真实地图）/ index.html（含客服浮窗）。

## 晚间再补充：真实微信二维码 + 部署上线 + 地图自有 Key
- 用户给真实微信二维码（剪贴板截图），保存为 `assets/images/wechat.png`；main.js 占位 SVG → `<img src="assets/images/wechat.png">`，css `.cs-qr svg`→`.cs-qr img`，文案改"扫一扫，添加微信咨询"。
- 地图切换为自有 Key 版本（非默认场景，合规）：contact.html 移除 `_TMapSecurityConfig.serviceHost` 代理，改为 `securityJsCode` 占位符 + SDK URL `?key=...` 占位符（按合规 skill 写 "Please apply for your own key at Tencent Location Service..."，URL 空格转下划线）；main.js 注释与 fallback 提示同步更新。
- 部署：用 cloudstudio-deploy 工具，目录=项目根（含 index.html，纯静态无需构建），端口 3000。首次 504 超时，重试成功。
- 部署结果：shareLink `https://56d755ed28f94d5684870b852cf22c00.app.workbuddy.link`，verified=true。
- 提醒：地图在公网显示的前提是在 contact.html 填入自有腾讯地图 Key + 安全密钥（https://lbs.qq.com 申请）；未填时灰屏并显示提示。客服浮窗/产品图/双语在公网均正常。

## 用户要"可编辑后台链接"
- 现状：纯静态站点无后端 CMS 后台。用 AskUserQuestion 给 3 方案（Decap CMS 可视化后台 / CloudStudio 在线编辑器 / 资料库后台），**用户选了 CloudStudio 在线编辑器**。
- 约束：cloudstudio-deploy skill 规则禁止透露 webIDE URL / sandboxId 等内部标识，故不能直接发 WebIDE 链接，只能指引用户在 CloudStudio 工作台进入该工作区编辑，改完由我重新 deploy 让线上生效。
